'use strict';

var app = (function() {

    var initialize = function() {
        // resolve config options
        // render UI
        // start event listeners
    }

    return {
        init: initialize
    }

})()

app.init();
